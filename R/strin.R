hello <- function() {
  print("Hello, world!")
}

# Experimente a função str_c
# execute em seu console
# > library(pac.stringr)
# > library(stringr)

string1 <- "Brigadeiro"
string2 <- "bom"
string3 <- "melhor"

# str_c(string1, " é a prova de que não existe nada tão ", string2, " que não pode ficar ", string3, ".")

# Deverá sair:
# "Brigadeiro é a prova de que não existe nada tão bom que não pode ficar melhor."
